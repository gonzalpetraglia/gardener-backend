if [ -z "$ENV" ] 
then
   export SIMPLE_SETTINGS='settings.default,settings.development'
else
   export SIMPLE_SETTINGS='settings.default,settings.'$ENV
fi

FLASK_APP=./src/main.py FLASK_ENV=development flask run --host=0.0.0.0
